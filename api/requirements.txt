fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pillow>=10.0
ultralytics>=8.3.0
# torch/torchvision do ultralytics kéo theo.
# Để chạy CPU nhẹ (không tải wheel CUDA lớn), cài torch CPU trước:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# (Dockerfile trong thư mục này đã làm sẵn bước đó.)
